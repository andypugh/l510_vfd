# l510_vfd


The component can be installed out-of-tree (ie into an existing LinuxCNC installation) with the command
```
sudo halcompile --userspace --install --extra-link-args="-lm -lmodbus" l510_vfd.c
```
